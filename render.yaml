services:
  - type: web
    name: mp3-downloader-backend
    env: docker
    plan: free
    dockerfilePath: backend/Dockerfile
    repoContext: backend

  - type: web
    name: mp3-downloader-frontend
    env: docker
    plan: free
    dockerfilePath: frontend/Dockerfile
    repoContext: frontend